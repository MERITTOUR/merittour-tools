{
  "name": "merittour-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "verify": "npm run check && npm test"
  }
}
