# MERITTOUR 사내 도구함 — 작업 규칙 (CLAUDE.md)

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 할 규칙을 정리한 것이다.
상세한 기술 명세는 `docs/dashboard_tech_spec.md`, 셋업·로드맵은 `docs/project_setup_guide_roadmap.md`를 참고한다.

## 저장소 개요
- 메리트투어(한국 골프 투어 여행사) 사내 자동화 도구함. GitHub Pages로 배포.
- 계정 `cmc338111-crypto`, 저장소 `merittour-tools` (public), 기본 브랜치 `main`.
- 진입점: `/sales/` (영업 도구 허브). 루트 `index.html`은 `/sales/`로 리다이렉트.
- 도구 경로: `tools/{toolname}/index.html`. 현재 도구: dashboard(주력)·insurance·imgtoolkit·weather·golfweather·library. (inquiry 폴더는 폐지됐으나 잔존. translate 는 삭제)
  - `golfweather`: 골프 라운딩 적합도(Open-Meteo 키리스). **로그인 없음**(손님 공개 + 브라우저 스토리지 미사용 스펙). 단일 자립형 HTML.
    - **이건 `weather`(리조트 날씨)의 대체 수단이다.** 리조트 날씨는 외부 사이트 `golfweather.info` 로 나가므로 그 사이트가 죽으면 같이 죽는다. golfweather 는 외부에 기대지 않고 직접 계산한다.
    - **영업 허브 카드로 두지 않는다.** 평소에는 자리만 차지하고, 정작 필요한 순간(외부 사이트가 안 열릴 때)에는 허브를 뒤지게 된다. 링크는 **리조트 날씨 안**에 「golfweather.info 가 안 열리나요?」로 붙여 뒀다 — 찾는 자리에 있어야 한다.
- 그 외 디렉토리: admin, air, manage, sales, assets, cards, shared, supabase.

## 페이지 진입은 로그인으로 막는다 (`shared/guard.js`)
- 모든 진입점 `index.html`은 `<head>`에서 본문보다 먼저 **한 줄**을 부른다. 깊이만 맞추면 된다.
  - sales·admin·air·manage: `<script src="../shared/guard.js" data-section="sales"></script>`
  - tools/* (2단계): `<script src="../../shared/guard.js" data-section="booking"></script>`
  - tools/library/archive (3단계): `<script src="../../../shared/guard.js" data-section="library"></script>`
  - 예외 3곳: 루트 `index.html`(권한에 맞는 허브로 보냄) · `login/`(무한 루프) · `tools/golfweather/`(손님 공개).
  - `data-section` 을 빼면 로그인·승인만 확인한다. `admin/`·`admin/users/` 가 그렇다(역할로만 연다).
- **의존 스크립트는 guard.js 가 스스로 부른다**(`supabase-config.js`·`access.js`). 페이지마다 세 줄을 맞춰 넣게 하면 어느 한 장에서 순서가 틀어져도 조용히 안 막힌다 — 한 줄이라야 빠뜨릴 자리가 없다.
- **확인이 끝날 때까지 본문을 그리지 않는다**(`body{visibility:hidden}`). 잠깐이라도 보이면 권한 없는 사람이 화면을 읽고, 로그인 화면으로 튕기는 깜빡임도 생긴다. `display:none`이 아니라 `visibility`인 이유는 레이아웃이 0이 되면 폭을 재서 그리는 표들이 잘못된 크기로 뜨기 때문이다.
- **실패해도 하얀 화면으로 두지 않는다.** 세션 없음 → 로그인 화면(`?next=` 로 돌아올 곳). 승인 전 → 이유 + [다른 계정으로 로그인]. 오류·설정 없음 → 이유 + [다시 시도]. 4초 넘으면 「확인하는 중」. 빈 화면이면 사람은 「고장」이라고만 알고 손쓸 데가 없다.
- **`?next=` 는 상대경로로 넘긴다.** 절대주소를 넘기면 로그인 화면의 열린-리다이렉트 방지에 걸려 늘 `/sales/`로 가버린다. 오리진을 정규식으로 떼지 말고 `new URL().pathname`을 쓸 것(`file://`로 열어 보는 경우가 있다).
- **문 앞에서 보는 것은 역할이 아니라 섹션이다**(`data-section`). 역할로 걸면 초대 기본값이 `air`라 신규 직원이 전부 막힌다. 역할은 섹션 권한의 **기본값**만 정하고, 누가 무엇을 여는지는 owner 가 계정마다 정한다.
- **루트(`index.html`)는 각자 볼 수 있는 허브로 보낸다.** 예전처럼 모두를 `/sales/`로 보내면 영업 허브 권한이 없는 사람이 문 앞에서 막힌다. owner·admin → `admin/`, 그 밖에는 `sales` → `manage` → `air` 중 처음 열리는 곳. 그래서 루트에는 `meta refresh`를 쓰지 않는다(권한을 확인하기 전에 튀어 버린다).
- **허브에서 볼 수 없는 카드는 감춘다.** 링크에 `data-section`을 적어 두면 `guard.js`가 지운다. 눌러 들어갔다가 막히는 것보다 안 보이는 편이 문의가 적다.
- 표시명은 **계정에서 온다**(`app_users.name`, 없으면 이메일 아이디). `MT_USER.get()`·`user()`·`role()`·`logout()`. 예전 gate.js의 `set()`/`prompt()`(자유 입력 이름 바꾸기)는 두지 않는다 — 각자 PC에서 고치면 「누가 올렸는지」가 사람마다 달라진다. 바꿀 곳은 `admin/users/` 하나다.
- 헤더 배지 메뉴에 **이메일과 역할을 적는다.** 공용 PC에서 앞사람 계정으로 남아 있는 것을 알아챌 자리가 거기밖에 없다.

### 왜 공용 비밀번호 게이트를 없앴나
- 예전에는 `shared/gate.js`가 비밀번호 **9800**(SHA-256 해시) + 자유 입력 이름을 받았다.
- 비밀번호 하나를 전 직원이 나눠 쓰면 ① 누가 들어왔는지 알 수 없고, ② 퇴사자를 막을 방법이 없고, ③ 한 번 새면 되돌릴 길이 비번 교체뿐이다. 이름도 자유 입력이라 gate.js 스스로 「진위 보장 X」라고 적어 두었다.
- 계정을 다 나눠 준 뒤에는 게이트가 지키는 것이 없었다. 오히려 **계정이 있는 사람도 9800을 따로 알아야** 도구에 들어가는 걸림돌만 남았다(로그인 화면에는 게이트가 없어서 로그인은 되는데 그다음이 막혔다).

## 로그인·역할 (Supabase)
- 진입은 `shared/guard.js`(로그인)가 막고, 실제 권한은 Supabase Auth + RLS다. 공용 비밀번호 게이트는 없앴다(위 절).
- 역할 5단계: `owner` > `admin` > `manage` / `sales` > `air`(읽기). **owner 는 무엇을 물어도 통과**한다(`can()`과 서버 `mt_has_role()`이 같은 규칙 — 안 그러면 화면엔 보이는데 저장이 막힌다).
- 계정은 콘솔에서 초대 → `app_users` 에 `air`·비활성으로 자동 생성 → **owner 가 `admin/users/` 에서 승인·역할·섹션 지정**.
- **섹션 권한**(`14_app_user_sections.sql`) — `app_users.areas`(읽기+쓰기) / `read_areas`(읽기만). 섹션 목록의 단일 출처는 `shared/access.js` 의 `SECTIONS` 이고, **마이그레이션의 키와 반드시 같아야 한다**(한쪽만 고치면 화면엔 보이는데 서버가 막는다). 판정은 화면 `canArea`/`canReadArea` 와 서버 `mt_has_area`/`mt_can_read_area` 가 같은 규칙 — **owner·admin 은 목록과 무관하게 전부 통과**하고, 나머지는 목록에 있어야 한다.
  - **쓸 수 있으면 볼 수 있다.** `areas` 에 있으면 `read_areas` 에 또 안 적어도 된다.
  - **관리자 화면은 섹션으로 두지 않는다.** 역할로만 연다 — 섹션으로 두면 owner 가 실수로 계정 관리 권한을 나눠 줄 수 있다.
  - **역할을 바꾸면 그 역할의 기본 섹션을 채워 준다**(`defaultsFor`). 안 채우면 `sales`로 올려도 섹션이 비어 아무 화면도 안 열린다 — 「승인했는데 왜 안 되지」가 된다. 채운 뒤 바로 저장하지 말고 무엇이 바뀌었는지 알리고 조정할 틈을 줄 것.
  - **본인이 자기 `areas` 를 못 채우게 `au_update_self` 가 두 컬럼을 고정한다.** 안 그러면 승인제가 뚫린다.
  - 사이젠 허브(`user_access`·`has_area`)에서 옮겨 왔지만 **세 가지는 고쳤다** — ① `manager`가 함수마다 다르게 취급되던 것(우회는 owner·admin 하나로 통일) ② `is_admin()`이 `active`를 안 봐 정지된 관리자도 통과하던 것 ③ 미등록자를 활성 `staff`로 돌려주던 것(승인제가 무력해진다).
- 첫 owner 만 SQL 한 줄(그 화면 자체가 owner 여야 열린다 — 닭·달걀).
- **계정 신청**(`15_access_requests.sql`) — 로그인 화면 「계정 신청하기」 → `access_requests` 한 줄 → owner 가 `admin/users/` 신청함에서 보고 **Supabase 콘솔 → Authentication → Users → Invite user** 로 초대(메일 발송) → 신청자가 링크에서 비밀번호 설정 → `app_users` 에 air·비활성 → owner 가 승인·섹션 지정.
  - **초대 메일을 코드에서 보내지 않는다.** 보내려면 `service_role` 키가 필요한데 공개 저장소에 둘 수 없다. 대신 신청함에 [이메일 복사]를 두어 콘솔에 붙여 넣는 수고만 남겼다. 자동화하려면 Edge Function 이 필요하다.
  - ⚠ **13 의 「anon 노출 0」 원칙에 대한 유일한 예외**다. 로그인 전 폼이라 anon 말고는 방법이 없다. 대신 **insert 만** 열고 select·update·delete 는 주지 않는다. `tests/sql-anon.test.mjs` 가 이 조건까지 검사한다 — 다른 표를 열거나 insert 밖의 권한이 붙으면 깨진다. **테스트를 느슨하게 풀어서 통과시키지 말 것.**
  - 같은 주소의 대기 신청은 부분 unique 인덱스로 하나만. 두 번 눌러도 줄이 늘지 않고, 화면은 「이미 접수되었습니다」로 안내한다(실패로 보이면 계속 다시 누른다).
- **`app_users.email` 은 `auth.users` 와 트리거로 맞춘다**(15). 04 의 트리거는 가입 시점에 한 번 복사할 뿐이라, 콘솔에서 로그인 이메일을 바꾸면 계정 관리 화면이 계속 옛 주소를 보여 줬다.
- **본인 역할 변경·본인 정지·마지막 활성 owner 내리기는 막는다.** 잠기면 아무도 계정을 못 고친다. 막을 때는 버튼만 끄지 말고 이유를 그 줄에 적을 것.
- **RLS 로 막히면 PostgREST 는 오류가 아니라 0행을 준다.** 0행을 성공으로 보면 조용히 안 저장된다 — `updateUser` 처럼 0행을 권한 안내로 바꿀 것.
- 접속 설정은 `shared/supabase-config.js`(URL + anon key). anon 키는 코드에 둬도 되지만 **`service_role` 키는 절대 넣지 않는다**.
- 초대 메일이 열리려면 콘솔 → Authentication → URL Configuration 의 **Site URL** 이 `https://merittour.github.io/merittour-tools/login/` 여야 한다(기본값 localhost 면 「사이트에 연결할 수 없음」).

## 엠클릭 통합 업로드 (서버 공유)
- `tools/register/` 에서 한 사람이 올리면 `data_registry` 에 출발월(`period`)별로 저장되고, 다른 직원은 파일 없이 쓴다.
- 저장하는 것은 **원본 행 그대로**(`res_json`·`ilhaeng_json`). 등록 페이지는 분석하지 않는다 — 파서를 두 곳에 두면 갈라진다. 달·팀수·인원만 세어 사람이 눈으로 확인하게 한다.
- 팀·인원 세는 기준은 대시보드 `analyze()` 와 **똑같이** 유지할 것(구분이 견적·대기·확정·정산이고 `cancCd` 없음). 다르면 「몇 건 줄었다」 경고를 믿을 수 없다.
- **날짜 칸은 반드시 `MT.encodeRows()` 로 바꿔 저장한다.** 대시보드는 `cellDates:true` 로 읽어 날짜가 `Date` 객체인데, 그대로 `JSON.stringify` 하면 `toISOString()` 이 불려 UTC 로 바뀌고 **한국(+9)에서 자정 날짜가 하루 앞으로 밀린다**(2026-07-15 → 2026-07-14). 불러올 때는 `MT.decodeRows()`.
- 같은 달을 다시 올리면 통째로 교체한다. 덮어쓸 때는 불러올 때의 `updated_at` 을 `expect` 로 걸어 **그 사이 다른 사람이 올렸으면 막는다**(안 막으면 나중 것이 조용히 이긴다).
- 블록표는 **원문(`block_raw`)만** 저장하고 행 해석은 대시보드가 한다.
- **블록표의 달은 절대 추측하지 않는다.** 엠클릭 「호텔별 예약현황」은 표 안에 월이 없고(`1(토)`·`2(일)`… 일자만) 출발년월은 표 바깥 셀렉터에 있어서 복사해도 따라오지 않는다. 올리는 사람이 직접 고르게 하고, 안 골랐으면 **올리기를 막는다**. 추측했다가 틀리면 7월 표가 8월 잔여로 들어가 손님에게 없는 방을 판다.
- 자료의 달과 블록표의 달이 다르면 **크게 알리되 막지는 않는다** — 일부러 옆 달 표를 올리는 경우가 있다.
- **올리는 곳은 대시보드 「자료 불러오기 → ＋ 새 자료 올리기」 하나다**(`regSaveUpload`). 올리고 보는 것이 갈라져 있으면 매번 페이지를 왕복해야 해서 나눌 이득이 없었다. `tools/register/` 는 리다이렉트만 남겼고 영업 허브 카드도 뺐다.
- **「내 화면에서만 열기」 같은 개인 상태를 만들지 않는다.** 다 같이 쓰는 공통 업무 프로그램이라, 나만 보는 자료가 생기면 사람마다 다른 숫자를 말하게 된다. 올리면 곧바로 모두에게 공유된다.
- 줄어드는 방향만 경고한다 — 늘어나는 건 추가 예약이라 정상이고, 줄어드는 건 분할 파일을 빠뜨렸을 때가 대부분이다.

## 배포
- **작업이 끝나면 묻지 말고 바로 `main`에 반영한다.** GitHub Pages는 `main`에서 배포되므로, 작업 브랜치에만 두면 실제 페이지에 안 올라간다.
- 흐름: 작업 브랜치에 커밋·푸시 → PR 생성 → CI(`verify`) 통과 확인 → 즉시 머지. 배포 여부를 되묻지 않는다.
- 다만 **되돌리기 어렵거나 손님에게 바로 나가는 변경**(신규 공개 페이지, 미완성 WIP 공개 등)은 무엇이 함께 배포되는지 한 줄로 알린다.

## 핵심 작업 원칙
- **코드가 문서보다 우선한다.** 문서가 GitHub 최신본과 다르면 실제 코드 파일을 기준으로 한다.
- **단일 HTML 파일 바이브 코딩.** 외부 라이브러리는 CDN(cdnjs)에서 로드. 로고는 인라인 SVG.
- **설계 먼저 제안 → 확인 → 구현.** 구조·디자인 변경은 먼저 제안하고 확인받은 뒤 구현.
- 데이터 저장은 localStorage. 키 네임스페이스: `mt_notify_*`, `mt_settle_*`, `mt_doc_*`, `mt_edit_*`, `mt_agency_*`.
- **라이트 모드 고정.** `@media (prefers-color-scheme:dark)` 분기를 넣지 말 것 — 같은 화면이 기기 설정에 따라 다른 색으로 갈려 보여 「어느 쪽이 맞느냐」를 되묻게 된다(2027 안내 리다이렉트 두 장이 실제로 그랬다).

## 검증 (납품 전 필수)
1. **JS 문법 검사**: 인라인 `<script>`를 추출해 `new Function(code)`로 파싱 오류 확인.
2. **실제 데이터 스모크 테스트**: jsdom-stub(가짜 DOM) 환경에서 실제 엑셀(예약리스트·일행별예약)을 읽어 `analyze()`를 돌리고 결과를 검증. SheetJS(xlsx)로 파싱.
   - ctx에 MutationObserver/IntersectionObserver/ResizeObserver/atob/btoa/scrollTo 등을 반드시 포함해야 스텁이 끝까지 돈다.
3. 수정 후 바로 끝내지 말고 위 1·2를 거친 뒤 결과를 보고할 것.
4. **CI 자동화**: push/PR마다 `.github/workflows/ci.yml`이 `node scripts/check-syntax.mjs`(인라인 `<script>`+`.js` 문법 검사)와 `node --test tests/**/*.test.mjs`(단위 테스트)를 실행. 로컬 검증은 `npm run verify`.
   - 공통 순수 함수의 단일 진실원은 `shared/util.js`(MT 네임스페이스, UMD — 브라우저 전역+Node require). 새 공통 로직은 여기에 모으고 `tests/util.test.mjs`에 테스트 추가.
   - `package.json`에 `"type":"module"`을 넣지 말 것(.js=CJS·.mjs=ESM 유지).

## 출력·파일 규칙
- 다운로드 파일명은 **반드시 영문** (한글 파일명은 다운로드 실패). 파일 내부 내용은 한글 유지.
- 대용량 한글 HTML 편집은 Python 배치 치환이 str_replace 반복보다 안정적.
- 한글 HTML 읽기는 `errors='replace'` 옵션 필수.

## dashboard 섹션 구조 (코드 기준 = 유일한 정답)
자료 불러오기 → ① 리조트 마스터 → ② 호텔 블록 → ③ 데이터 검수 → ④ 요금 관리 → ⑤ 예약 관리 → ⑥ 항공 정보
→ (준비중) 확정서 생성 · 정산 · 보험 · 송영 · 숙소 · 골프 · 식사
- 활성 6개(①~⑥)에 원형 번호. 준비중 7개는 「준비중」 배지(단 analyze 시 unlock되어 클릭·동작).
- 확정서·정산은 기능 완성도 높으나 UI 마감·배포 정책상 준비중으로 분류(추후 활성 승격 예정).
- 과거 ①~⑪ 단일 번호 체계는 폐기됨.
- **등록된 달은 칩으로 늘어놓는다.** 드롭다운이면 몇 달이 올라와 있는지 열어 봐야 알고 고른 뒤 버튼을 또 눌러야 한다. 칩에는 달·올린 사람·시각·팀·인원·블록표 유무를 함께 적는다.
- **「＋ 새 자료 올리기」는 접지 않는다.** 매번 펼치는 동작이 늘어날 뿐이다.
- **첫 화면은 파일 업로드가 아니라 「자료 불러오기」다.** 열면 등록된 달이 자동으로 다 합쳐져 `analyze()`까지 돈다.
- 불러온 뒤에는 반드시 **`MT.decodeRows()`로 되돌린다.** 안 되돌리면 `Date`를 기대하는 곳이 문자열을 받는다.
- 블록표를 함께 불러올 때는 `blk-month`도 같이 맞춘다 — 안 맞추면 7월 표를 5월 달력에 그린다.

## 여러 달 누적 보기 (등록하면 그대로 보인다)
- **다같이 같은 자료를 보는 시스템이다. 등록된 달은 항상 전부 합쳐져 있다.** 고르는 개념도, 켜고 끄는 개념도 없다.
- **칩은 누르는 것이 아니라 보는 것**이다(무엇이·누가·언제 올라와 있는지, 블록표가 있는지). 한때 눌러서 켜고 끄게 했으나 예전 「한 달 고르기」의 잔재였고 정당화되지 않았다 — ① 12달을 다 받아도 gzip 0.3MB·`analyze()` 0.16초라 끌 이유가 성능에 없고, ② 달을 끄면 그 달이 **잔여 계산에서 통째로 빠져 없는 방을 팔게 되고**, ③ 사람마다 다른 달을 켜 두면 **사람마다 다른 숫자를 말하게 된다**(「내 화면에서만 열기」를 없앤 이유와 같다).
- 기간을 좁혀 보고 싶으면 **각 탭의 날짜 필터**를 쓴다 — 그건 계산 대상을 바꾸지 않고 보기만 바꾼다. 재고·잔여에 영향을 주는 축소는 만들지 말 것.
- 여러 달은 `MT_STORE.loadMany()`로 **한 번에** 받는다(달마다 부르면 왕복이 그만큼 늘어난다). 받은 달은 `regCache`에 남고, `regMonths()`(= 캐시에 있는 달)가 곧 화면이다.
- **다른 사람이 올린 것을 반영하는 길은 「새로고침」(`regRefreshAll`) 하나다.** 목록과 자료를 다시 받아 캐시를 갈아 끼운다. 자동 감지는 아직 없다.
- **목록에는 있는데 자료를 못 받은 달은 드러낸다** — 칩에 「못 받음」, 요약 줄에 이유. 조용히 빼면 팀 수가 왜 모자란지 알 길이 없다.
- **저장은 달 단위 통째 교체를 유지한다.** 엠클릭 예약리스트는 그 달의 현재 상태 전부(스냅샷)라, 행 단위로 쌓으면 **취소돼 사라진 예약이 영원히 살아남아** 없는 손님이 명단에 남고 방이 잡힌 것으로 계산된다. 누적은 **화면에서만** 한다.
- **같은 `eventSeq`가 두 달에 보일 수 있다** — 일정이 다른 달로 옮겨졌는데 옛 달을 아직 다시 안 올렸을 때다. `updated_at`이 **최근인 달**이 이기고, 명단(`ilhaeng`)도 이긴 달 것만 쓴다. 겹쳤다는 사실은 **화면에 적는다**(`regDupSeqs`).
- 지금 무엇이 합쳐져 있는지 **요약 줄에 적는다**(`#reg-sumline`). 화면의 숫자가 어디까지를 센 것인지 모르면 매번 되묻게 된다.
- 블록표는 **등록된 달 전부**를 `vcalBlocks`에 넣고, 그 기간의 **앞뒤 한 달**은 블록 칸만 더 받는다(`regLoadEdgeBlocks`). 붙여넣기 칸(`blk-paste`)은 한 달치만 보여 준다.
- **올리려고 고른 파일은 다른 그릇(`upRes`/`upIlh`)에 담는다.** 화면 데이터와 한 그릇에 담았더니, 여러 달을 합쳐 본 상태에서 업로드 요약이 「팀 4,116건 — 통째로 교체됩니다」로 적혔다. 실제로는 한 달만 바뀌는데 전부 갈아엎는 것처럼 보였다.

## 탭 렌더는 열 때 한 번 (지연 렌더)
- `analyze()`는 **탭을 그리지 않는다.** `secMarkAllDirty()`로 표시만 하고, 보고 있는 탭만 `secRender(secCurrent)`로 즉시 다시 그린다. 나머지는 `gotoSection()`이 그 탭을 열 때 처음 그린다.
- 이유: 예전에는 `analyze()`가 끝에서 전부 그렸는데 **12개월(팀 4,116건)에서 8.5초**가 걸렸고 그중 **7.4초가 지금 보고 있지도 않은 탭**이었다(항공 2.6s·보험 1.0s·예약 0.84s·송영 0.79s…). 지연 렌더로 **0.16초**가 됐다.
- 안전한 이유: 렌더 함수들은 **DOM만 건드리고 전역을 바꾸지 않는다.** 새 렌더 함수를 넣을 때 전역을 쓰면 이 전제가 깨진다 — 계산은 `analyze()`에 두고 렌더는 그리기만 할 것.
- **`analyze()`에서 탭을 자동으로 옮기지 않는다.** 예전에는 검수로 넘겼는데, 이제 화면을 열자마자 자료가 합쳐지므로 사용자가 「자료 불러오기」를 보지도 못한 채 튕긴다 — 어느 달이 켜져 있는지, 올린 결과가 어떤지 적어 둔 자리가 바로 그 화면이다.
- 전 탭에 영향을 주는 변경(일괄 수정 등)은 `secRefreshAll()`을 쓴다 — 보는 탭만 그리고 나머지는 미룬다.

## 엠클릭 데이터 (2종 통합 완료)
- 입력 2종: **예약리스트**(예약현황 탭) + **일행별예약**(탑승자정보 탭). 둘 다 출발일별/예약일별에서 다운로드. **예약리스트는 500행씩 분할**, **일행별예약은 1개월 단위로 명단 인원수와 무관하게 한 번에** 받음 → 여러 달/분할분은 같은 칸에 여러 파일 올리면 자동 병합.
- 결합 키: `eventSeq`(silent join). 표시 키는 행사번호.
- 일행별예약 한 파일에 항공시각 4종·PNR·항공사·출발/귀국편·사전좌석·여권 정보 모두 포함(구 APIS·현지도착 폐지).
- 비고 3종: 비고·현지 비고·기타 비고. 예약리스트 idx 30·31·32.
- PNR은 콤마(전각 ， 포함) 분리 시 전부 기재(첫 개만 쓰면 안 됨).

## 거래유형(일반/여행사 B2B) 전역 필터
- 상품명이 "여행사"로 시작 = B2B(`parseProduct`의 `isAgency`).
- 전 탭 공유 필터 `agencySel`(localStorage `mt_agency_filter`, PC별 기억, 기본 일반만, 다중선택).
- 헬퍼: `agencyOk(t)`, `agencyBadge(t)`. 예약·항공·요금관리·확정서·정산에 칩 필터+배지, 검수는 배지만.

## 호텔 블록 보유
- 보유의 단일 출처는 리조트 마스터. 룸타입별 **기본 보유**(`rooms`) + **기간 예외**(`roomExceptions`: 룸타입·시작일·종료일·수량·메모).
- 겹치면 **짧은 기간이 우선**, 길이가 같으면 나중 등록. 동률 겹침은 마스터 카드에 경고 표시. 순수 로직은 `shared/util.js`의 `MT.roomCapOn` 등(테스트 있음).
- 날짜별 조회는 `blkBlockOn(hotel,room,ymd)` / `blkPoolCapOn(pool,ymd)`. 정원 등록 여부 판정만 `blkPoolCapBase(pool)`. 기존 `blkBlockOf`·`blkPoolCap`은 기본값 조회로 존치.
- **새 소비처에서 잔여를 계산할 때는 반드시 날짜판을 쓸 것.** 특히 연박 최소 잔여(출발 일정 조회·공지 PNG·요금달력)는 하루라도 빠뜨리면 틀린 값이 손님에게 나간다.
- 오버부킹 보정(`mt_notify_blkOver`)은 내 PC 전용 임시 시뮬레이션 — 마스터 내보내기에 포함되지 않는다. 실제 보유 변경은 기간 예외로 넣어야 전 직원이 공유한다.
- **블록 현황 표의 행은 마스터가 아니라 붙여넣은 엠클릭 표(실제 사용)에서 나온다.** 단 룸타입 별칭이 걸린 표기는 대상 룸타입 행에 **사용이 합산**된다(합치지 않으면 같은 보유가 두 행에 붙어 이중 계산된다).
- 마스터에서 룸타입을 지워도 예약이 잡혀 있으면 행은 남고 「보유미등록」이 된다 — 동기화 실패가 아니라 실제 불일치다. **그 행 안의 인라인 컨트롤**(합치기/마스터 추가)로 바로 해소한다.
- **숙소 자체가 안 붙은 경우(「숙소 미등록」)도 같은 행에서 해소한다.** 행 안의 숙소 드롭다운 + 「연결」이 그 표기를 마스터 `aliases`에 등록한다(`blkFixHotelInline`). 아래 경고 카드는 요약용이다.
- **숙소명 매칭은 띄어쓰기·대소문자를 무시한다**(`blkCanonHotel` → `blkNormName`). 「구마모토 시내 호텔」과 「구마모토시내호텔」이 마스터에 있는데도 「숙소 미등록」으로 떨어지던 것을 막는다. 그보다 크게 다른 표기는 자동 추측하지 말고 행 안에서 사람이 연결한다.
- **숙소 배치 순서의 단일 출처는 `ACCOM_ORDER`**(코드 상수). 블록 현황 표·호텔 필터 칩·현황 엑셀이 모두 `지역(마스터 순서) → ACCOM_ORDER → 이름`으로 정렬한다. 목록에 없는 숙소는 뒤로 밀리고 이름순으로 붙는다 — 숨기지 않는다.
- **룸타입 별칭은 마스터에 있는 룸타입으로만 돌린다.** `blkResolveRoom`은 별칭 대상이 마스터에 없고 원래 표기는 있으면 별칭을 무시한다. 안 그러면 있는 보유를 버리고 없는 룸타입으로 옮겨 「보유미등록」이 된다(기본값 별칭 트윈→맨션이 트윈만 등록된 PC에서 실제로 그랬다).
- 상세: `docs/design_master_block_exceptions.md`

## 여러 달 연결 (월 경계)
- 등록월은 `vcalBlocks`(localStorage `mt_notify_blkMonths`, 3개월 보관). 「요금 달력에 이 달 등록」으로 쌓는다.
- **연박 계산은 날짜 기준**(`blkUsedOn`·`blkSpanRemain`). 달을 넘어도 끊기지 않는다. 다음 달 미등록이면 조용히 빼지 말고 「판단 보류」로 드러낼 것 — 예전에 월말 출발편이 목록에서 사라지던 원인이다.
- 손님 공지 PNG는 보류 건을 넣지 않는다(확인 안 된 일정 안내 금지).
- 표·오버부킹 보정 키는 **날짜(YYYY-MM-DD)**. 일자(DD)로 두면 이어보기에서 7/15와 8/15가 겹친다.
- 사용이 0인 달도 등록 가능해야 한다(파서가 사용>0만 행으로 만들므로 빈 배열 = 정상 상태).
- 이어보기는 인접 2개월. 상세: `docs/design_multi_month_block.md`
- **엠클릭 블록표는 한 달치만 복사된다.** 그래서 예전에는 월 경계를 보려면 두 달을 각각 붙여넣고 「요금 달력에 이 달 등록」을 손으로 눌러야 했고, 안 누르면 조용히 끊겼다. 지금은 서버에 달마다 표가 따로 올라가므로, **대시보드가 한 달을 불러올 때 앞뒤 달 블록표까지 자동으로 받아** `vcalBlocks`에 채운다(`MT_STORE.loadBlock` — 블록 칸만 받아서 옆 달 예약 원본을 또 내려받지 않는다).
- 옆 달이 아직 안 올라왔으면 **조용히 이 달만** 쓴다 — 오류로 만들지 않는다. 다만 무엇을 더 받았는지는 불러오기 안내에 적는다.

## 룸타입 정원 (인원/방)
- 룸타입마다 `roomMeta:{룸타입:{pax, online}}` — `pax`=1방 정원(명), `online`=온라인 예약 노출 여부(연동 대비 데이터, 현재 대시보드 동작에 영향 없음).
- **인원을 객실 수와 직접 비교하면 안 된다.** 골프투어는 2인 1실이 기본이라 4명 팀은 트윈 2실이면 된다. `MT.roomsNeeded(pax, per)`로 환산할 것.
- 정원 미등록이면 `null` → 1인 1실로 **보수적으로** 처리하고 화면에 미등록임을 표시한다. 조용히 낙관적으로 잡으면 없는 방을 팔게 된다.
- 인원→방 조합은 `MT.roomPlans(pax, types)` (빈자리 0인 조합만이 기본, `{exact:false}`로 빈자리 허용). 온라인 선택 가능 인원은 `MT.bookablePax`.
- 룸타입 이름 변경·삭제 시 `rooms`·`roomMeta`·`roomExceptions`·`roomAlias` 네 곳을 함께 옮기거나 지울 것.
- **룸타입 매칭 키는 이름 문자열**이고 마스터와 엠클릭 표가 각각 정한다. 마스터에서 이름만 바꾸면 붙여넣은 표가 「보유미등록」이 되므로, 이름 변경 시 옛 표기를 `roomAlias`(숙소별 `{엠클릭 표기: 마스터 룸타입}`)에 자동 등록한다. 숙소명의 `aliases`와 같은 역할.
- **재고 단위는 객실이다.** 인원 케파로 바꾸지 않는다 — 호텔 계약이 방 단위라, 3명이 2실을 쓰면 인원은 3만 줄지만 방은 2개가 빠진다. 임의배정형(`assign:true`)만 예외.
- 빈자리 요금(2인실에 1명 등)은 온라인에서 계산하지 않고 **사후 수기 정리**. 그래서 홀수 인원도 받을 수 있다.
- 회원권 예약가능인원은 2층 구조: `commonMaster.memberPax`(회원권구분별 기본) < 엠클릭 고객관리의 **개인별 「예약가능인원」**(우선). 개인별 값 연결은 결합 키 미정.
- 상세·미확정 사항: `docs/design_room_capacity_booking.md`

## 요금·환율
- 회원관리비: 리조트 마스터 숙소별 입력값이 단일 출처(기본 50,000원/인, 면제 숙소만 0).
- 환율: 하나은행 현찰 살 때 최초 고시. 기준일이 **출발 시즌마다 다르다** — **2026년 출발 = 출국 2달 전** / **2027년 출발부터 = 3달 전** 월 1일자(2027 시즌 변경). 월별 테이블만 사용(미등록 월은 검증 제외).
  - 3달 전으로 바뀐 이유: 확정서를 **출발 3~2개월 전**에 확정요금과 함께 보내는데, 기준이 2달 전이면 3개월 전 시점엔 환율이 없어 확정서를 못 만든다. 근거 문서 = 사내 「2027년 회원 예약 가이드 추가 문안」·「2027년 예약 운영 내부 매뉴얼」.
  - 이 기준은 `/reserve/spec/`(요구사항 정의서 §6.3)에도 같은 값으로 적혀 있다. **한쪽만 고치지 말 것.**
- 항공요금 매칭은 IATA 코드 정규화(`airportCode`). 도착지는 숙소 arrCode → APIS 도착지 순.
- 지역명과 실제 공항 다른 상품 주의: 벳푸=후쿠오카(FUK), 이바라키=나리타(NRT), 나가노=니가타(KIJ).

## 디자인 토큰
- dashboard UI: 베이스 #EEF2F8, 액센트 #2E5A88, 헤더 네이비 #1a2332. Pretendard 17px 본문, Noto Serif KR 헤더.
- 구분값 신호색(tag-status-*): 견적 노랑·대기 보라·확정 파랑·정산 초록·취소 빨강.
- 베이지·골드 계열 비선호. 고객 발송물은 JPG, Noto Sans KR.

## 마스터 배포 흐름 (서버 단일 출처)
- **리조트 마스터는 서버에 하나만 있다**(`resort_master` 단일 행). localStorage(`mt_notify_resortMaster`)는 첫 화면을 그리기 위한 **마지막-정상-사본(캐시)**일 뿐이고, 서버 응답이 오면 통째로 갈아 끼운다.
- 상태는 셋뿐이다. `masterMode`:
  - `off` — 서버에 못 닿았다(미설정·로그인 전·장애). 예전처럼 이 PC 값으로 돈다. **오류 화면으로 만들지 않는다.**
  - `unseed` — 서버는 닿았는데 마스터가 비어 있다. **편집을 잠그고** owner 를 기다린다.
  - `on` — 서버 값이 정답. 편집·저장이 서버로 나간다.
- **미시드 판정은 「행이 없으면」이 아니라 「`data.resortMaster` 가 비었으면」이다.** 서버 행은 처음부터 `data='{}'` 로 존재하므로 null 검사는 영원히 안 걸린다.
- **자동으로 씨앗을 심지 않는다.** owner 가 「이 PC 값을 서버 마스터로 등록」을 한 번 눌러야 한다. 자동으로 하면 ① 여러 PC 가 각자 값을 밀어 넣어 마지막에 연 사람이 이기고, ② 누구 값이 기준인지 아무도 모르고, ③ 브라우저 데이터를 지운 PC 는 **코드 기본값(그린피 0·식사 0)** 을 밀어 넣어 전 직원 견적을 망친다.
- **읽기 경로에서 절대 저장하지 않는다.** 예전에 1회성 마이그레이션이 `loadResortMaster()` 안에서 `saveResortMaster()` 를 불렀는데, 서버 공유가 되면 그것은 「새 PC 가 화면을 열기만 해도 공유 마스터에 쓰기가 나가는」 사고가 된다. `masterFromPayload()` 는 순수 함수로 유지할 것.
- 저장은 **2초 모아서** 한 번 보낸다(`masterPushServer`). 호출부가 28곳이라 칸마다 나가면 단일 행에서 거의 매번 충돌한다.
- **0행은 두 가지다.** 그 사이 남이 저장(충돌) / RLS 로 막힘(권한). `saveMaster` 가 version 을 다시 읽어 가른다 — 뭉뚱그리면 권한 없는 사람이 원인에 닿지 못한 채 같은 값을 계속 다시 입력한다.
- **충돌 시 자동으로 다시 밀지 않는다.** 마스터는 통짜 JSON 한 벌이라 서버 값을 받아 그대로 덮으면 남의 수정이 **조용히** 사라진다. 사람이 「서버 값으로 되돌리기」/「내 값으로 덮어쓰기」를 고른다.
- **저장이 막혔으면 화면이 그렇게 말해야 한다.** 「전 직원에게 바로 반영됩니다」를 띄워 둔 채 실패하면, 안 저장된 값으로 견적을 낸다.
- 「마스터 불러오기」는 **`masterMode==='on'` 이 되면 화면에서 빠지고 함수에서도 막힌다** — 서버가 단일 출처가 된 뒤에는 「석 달 전 JSON 하나로 전 직원 단가를 되돌리는 버튼」이 되기 때문이다. 다만 그 전에는 **유일한 공유 수단이라 미리 지우면 안 된다**(SQL 실행 전 배포 구간에 마스터를 나눌 방법이 없어진다). 내보내기는 **백업 전용**이고, 되돌리기는 서버 스냅샷(`resort_master_history`)으로 한다.
- **코드 소유 항목은 저장본이 덮지 못한다.** `masterFromPayload()`는 `{...기본값, ...저장본}`으로 병합하므로, UI에 수정 수단이 없는 항목(`name`·`region`·`golf`·`holes` = `MASTER_CODE_OWNED`)은 병합 후 **항상 기본값으로 되돌린다.** 서버 단일 출처가 돼도 이 병합은 남긴다 — `name` 은 블록 매칭 조인 키라 서버 값으로 얼리면 코드에서 숙소명을 바꿔도 아무에게도 반영되지 않는다.
- `aliases`는 사용자가 편집할 수 있어 **기본값 ∪ 저장본**(합집합)으로 합친다. `roomAlias`는 기본값 밑에 저장본을 얹어 직접 등록분이 이긴다.
- 룸타입 **구조**를 바꿀 때는 `ROOMS_BEFORE_RENAME`에 바뀌기 전 기본값을 적고, 저장본이 그 값 그대로일 때만(= 손대지 않았을 때만) 새 구조로 올린다.
- 룸타입 이름만 바뀌는 경우는 기본값의 `roomAlias`에 옛 표기를 넣어야 붙여넣은 엠클릭 표가 계속 매칭된다.

## 변경 이력 (누가·언제·무엇을)
- 서버 `mt_change_log` 한 벌에 쌓고 **전 직원이 같이 본다.** 예전에는 내 PC 에만 쌓였고 「누가」가 아예 없어서, 남이 무엇을 고쳤는지 볼 방법이 구조적으로 없었다.
- **「누가」는 서버가 박는다**(`mt_mcl_stamp` 트리거가 `auth.uid()` 로). 게이트 이름(`MT_USER.get()`)은 본인이 타이핑한 자유 문자열이라 주체로 쓸 수 없다 — `gate.js` 스스로 「진위 보장 X」라고 적어 두었다.
- **값 저장이 성공한 뒤에만 보낸다**(`masterFlushLog`). 저장이 막혔는데 이력만 남으면 이력이 거짓말을 한다.
- **모아서 한 번에 보낸다.** 일괄 수정 300건을 300번 왕복하면 안 된다.
- **지우는 기능을 만들지 않는다.** 서버에도 `update`/`delete` 정책이 없다. 오등록은 지우는 대신 반대 방향으로 한 줄 더 남긴다.
- 아직 못 보낸 것은 `masterPendingLog` 에 남고 다음 저장 때 다시 시도한다. 화면에는 「·」로 표시한다.

## 개인 업무값을 서버로 (행사 단위 덧칠)
- `mt_event_overlay` 한 표에 **eventSeq 하나를 키로** 모은다 — 구분·호텔Y·명단Y·비고·팀명·실제호텔명·요금 보정. 표를 나누면 예약 한 줄을 그리려고 다섯 번 조인하게 된다.
- **`구분`이 가장 위험하다.** 확정서 대상·정산 대상·**D-7 자동발송 대상**을 정한다(`docSyncSupabase` 가 `확정/정산` 팀만 `reservations` 로 올린다). PC 마다 다르면 어떤 손님에게 확정서가 나가는지가 사람마다 달라진다.
- 마스터와 달리 **낙관적 잠금을 걸지 않는다** — 행이 팀 단위라 통짜 문서가 아니고, 두 사람이 같은 팀을 동시에 고치는 일이 드물다.
- **자동으로 밀지 않는다.** 서버 값이 이 PC 값을 이기고, 이 PC 에만 있는 것은 지우지 않은 채 「이 PC 에만 있는 수정 N건 · 서버로 올리기」로 드러낸다. 자동이면 여러 PC 가 각자 밀어 넣어 마지막 사람이 이긴다(마스터 시드와 같은 이유).
- **바뀐 행사만 보낸다.** `savePersist()` 는 아무 데서나 불리므로 그때마다 전부 밀면 수백 행이 반복해서 올라간다 — `ovlLast` 와 비교해 실제로 달라진 것만 고른다.
- **덧칠이 비면 행을 지운다**(원본 엠클릭 값으로 복귀). 빈 행을 남기면 「덮어쓴 것」과 「원본 그대로」가 구분되지 않는다.
- 안 건드린 칸은 `null` 로 보낸다 — 원본을 덮지 않는다는 뜻이다.

## 웹 오픈 수량 (구 오버부킹 보정)
- `mt_block_override`(hotel·room·ymd → `qty_delta`). **전 직원이 함께 보는 값이다.** 화면에는 오래도록 「내 PC 에서만 쓰는 임시 시뮬레이션」이라 적혀 있었지만, 실제로는 블록 현황 표의 잔여·선택 합계·현황 엑셀·현황 PNG 에 전부 실려 나갔다 — 공유되는 값인데 사람마다 다른 값을 들고 있었다.
- **줄이는 방향도 저장된다.** 예전에는 `over>0` 만 저장해 **늘리기만** 됐다 — 잔여를 실제보다 많게만 만들 수 있었고, 그건 없는 방을 파는 방향이다.
- 조정이 0 이 되면 행을 지운다(업서트 뒤 `qty_delta=eq.0` 일괄 삭제). 0 짜리 행을 남기면 「조정한 것」과 「안 건드린 것」이 구분되지 않는다.
- **이 값은 「온라인에 몇 실을 열지」만 정한다.** 실제로 확보한 객실 수가 다르면 리조트 마스터의 **기간별 보유 예외**에 넣어야 한다 — 두 개념을 섞으면 재고가 어디서 왔는지 알 수 없게 된다.
- **잔여는 「보유와 웹오픈 중 적은 쪽」이다**(`blkSellableOn` → `blkPoolCapOn`). 웹오픈을 **늘려** 뒀어도 출발 일정 조회·손님 공지 PNG·요금 달력에서는 보유까지만 센다 — 늘린 숫자가 손님에게 나가면 **없는 방을 팔게 된다.** 반대로 **줄여** 뒀으면 줄인 만큼만 센다. 어느 방향으로도 실제보다 많게 나오지 않는다.
- 블록 현황 표·시뮬레이션은 운영용이라 `보유 + 조정`을 그대로 보여 준다(오버부킹을 일부러 연 것이 보여야 한다). 손님·영업 쪽으로 나가는 잔여만 위 규칙으로 좁힌다.

## 정산 조정·공제, 보험 제외 (서버)
- `mt_settle_adjust`(행사#인원) · `mt_settle_deduction`(id) · `mt_pax_overlay`(행사#인원).
- **공제는 현지에 실제로 보내는 송금액을 정한다**(실정산액 = 체재 + 송영 − 공제). 공제 행이 없는 PC 는 그만큼 **더 보낸다** — 개인 PC 에 둘 값이 아니다.
- **빈 행을 남기지 않는다.** 업서트한 뒤 「아무것도 안 적힌 행」을 한 번에 쓸어낸다(`air_cost=is.null&…&ex_stay=is.false`, `ins_excluded=is.false`). 0·null 만 남은 행을 두면 「조정한 것」과 「안 건드린 것」이 구분되지 않는다.
- **공제 id 는 시각+난수다.** `Date.now()` 만 쓰면 두 PC 가 같은 순간에 추가할 때 겹쳐 한쪽이 사라진다. 렌더에서 id 를 따옴표로 감쌀 것(문자열이다).
- `saveSettle()` 은 칸을 칠 때마다 불리므로 **실제로 달라진 것만** 고른다(`setlAdjLast`·`setlDedLast`).
- **`loadPersist()` 는 `analyze()` 안에서만 불린다.** 서버 동기화가 로컬 값을 읽어야 하면 그 앞에서 직접 한 번 부를 것 — 안 그러면 자료가 없을 때 맵이 비어 있어 「이 PC 에만 있는 값」이 0 으로 나오고, 자료가 있어도 `analyze()` 와의 순서에 따라 달라진다.

## 거래유형(일반/여행사) 필터 — 기본은 전체
- **기본값은 전체이고 PC 에 기억하지 않는다.** 예전에는 「일반만」이 기본이고 PC 별로 기억까지 해서, **여행사(B2B) 확정 팀이 확정서 목록에 아예 뜨지 않았고 일괄 발송 수신자에서 조용히 빠졌다.** 그 손님만 확정서를 못 받는다.
- 좁혀 보는 것은 그 세션 동안만 하고, 다음에 열면 전체로 돌아온다.

## 접속·발송 설정은 코드에 있다
- Supabase URL·anon key·버킷·알림톡 주소를 **PC 마다 입력하던 칸(`mt_doc_cfg`)을 없앴다.** URL·키는 이미 `shared/supabase-config.js` 에 있고, 알림톡 주소는 `MT_SB.URL + '/functions/v1/send-alimtalk'` 로 만든다.
- 안 채운 PC 는 확정서 링크가 안 열리는 것으로 끝나지 않았다 — `analyze()` 끝의 자동 동기화가 **조용히 건너뛰어져 D-7 알림톡이 낡은 자료로 나갔다.**
- **동기화 실패를 삼키지 않는다.** 자동 실행(silent)일 때도 화면에 남기고, 무엇이 걸린 일인지(D-7 자동발송) 함께 적는다.
- 입금계좌·문의전화는 `commonMaster.docAcct`(서버 마스터)다. **손님 문서에 찍히는 값**이라 PC 마다 다르면 안 된다 — 안 넣은 PC 에서 뽑은 확정서는 계좌가 「별도 안내드립니다」로, 문의 전화는 **빈칸**으로 나갔다.
- `tools/translate`(문서 번역)와 `functions/translate` 는 **폐지했다** — 안 쓰기로 함. 저장소·영업 허브·Edge Function 모두에서 지웠다. Supabase 콘솔의 함수도 함께 삭제할 것(코드에서 지워도 서버에 떠 있으면 주소만 알면 계속 불린다).

## 손님 데이터 경로는 로그인 토큰으로
- 확정서 저장(Storage)·예약 동기화(`reservations`)·알림톡 발송은 **로그인한 사람의 토큰**으로 나간다(`docAuthHeaders`). 토큰이 없으면 **보내지 않는다** — 조용히 anon 으로 흘러가면 안 된다.
- anon 키는 public 저장소에 원문으로 있다. 정책이 `using(true)` 로 열려 있으면 그 키만으로 브라우저 없이도 손님 예약을 읽고 쓰고 확정서를 내려받을 수 있다.
- **순서를 지킬 것**: ① 코드를 토큰으로 배선 → ② `12_reservations_auth.sql`(authenticated 열기, anon 유지) → ③ 도는 것 확인 → ④ `13_lock_anon.sql`(anon 닫기). 반대로 하면 자동 동기화가 **조용히** 멎어 D-7 알림톡이 낡은 자료로 나간다.

## Edge Function 은 fail-closed
- `cron-d7-alimtalk` 는 `if (secret && …)` 라 **`CRON_SECRET` 을 안 넣으면 검사를 통째로 건너뛰었다.** 주소만 알면 누구나 전 고객에게 D-7 알림톡을 쏠 수 있었다 — 이제 시크릿이 없으면 **막는다**.
- `send-alimtalk` 은 검사가 아예 없었다(폐지한 `translate` 도 같았다). 로그인 토큰을 받아 `/auth/v1/user` 로 직접 확인한다. **환경변수가 비어 있으면 막는다** — 설정을 빠뜨렸을 때 열리는 쪽으로 기울면 안 된다.
- 새 함수를 만들 때도 같은 규칙: 인증 정보가 없으면 통과가 아니라 거절이다.

## 협업
- 차분하고 정직한 피드백 환영. 과도한 칭찬 불필요. 한국어 존댓말.
